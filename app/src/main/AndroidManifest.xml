<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.memomap">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MemoMap"
        tools:targetApi="31">

        <!-- Main Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity
            android:name=".CalendarHomeActivity"
            android:exported="false" />

        <activity
            android:name=".CalendarRecap"
            android:exported="false" />

        <activity
            android:name=".FullMonthActivity"
            android:exported="false" />

        <activity
            android:name=".OneLineActivity"
            android:exported="false" />

        <activity
            android:name=".ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".AvatarActivity"
            android:exported="false"/>




        <!--                <activity android:name=".CalendarRecap" android:exported="false" />-->

        <!--        <activity android:name=".LoadingRecap" android:exported="true">-->
        <!--            <intent-filter>-->
        <!--                <action android:name="android.intent.action.MAIN" />-->
        <!--                <category android:name="android.intent.category.LAUNCHER" />-->
        <!--            </
        intent-filter>-->
        <!--        </activity>-->

        <!-- <activity -->
        <!-- android:name=".TestingActivity" -->
        <!-- android:exported="false" /> -->
        <!-- Coba ini gua ya -->
        <!--        <activity-->
        <!--            android:name=".AvatarActivity"-->
        <!--            android:exported="true">-->

        <!--            &lt;!&ndash; This makes AvatarActivity the app's entry point &ndash;&gt;-->
        <!--            <intent-filter>-->
        <!--                <action android:name="android.intent.action.MAIN" />-->

        <!--                <category android:name="android.intent.category.LAUNCHER" />-->
        <!--            </intent-filter>-->
        <!--        </activity>-->
    </application>

</manifest>